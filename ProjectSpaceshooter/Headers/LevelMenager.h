#pragma once
#include "GameData.h"
#include "LevelDescription.h"

/** Level manager class
  *
  * @author Zyga
  */
class LevelMenager
{
public:
	LevelMenager(void);
	void update();
	~LevelMenager(void);
};

